# 98KeyChk 引き継ぎ書

作成日: 2026-08-08

## 現在の目的

Neko Project 21/W上のMS-DOSおよびPC-98実機で動作する、キーボード入力確認プログラムをLSI C-86 Ver.3.30c 試食版で開発する。

キーボード配置の表示、押されているキーの反転表示、通常キーボードとWIN・APPキー付きキーボードの手動切り替えは実装済みである。実機で判明したキーボード通信、キーバッファー溢れ、機種差への対処を反映し、正式公開バージョンをVer.1.0としている。

## 主要な実装方針

- Git管理する原本は `src/` に配置する。
- `src/` 内のCソースとヘッダーファイルはUTF-8、付属説明書 `src/98KEYCHK.TXT` はPC-98上でも読めるようShift_JISで管理する。
- `.c` と `.h` はCRLF、MarkdownはLFとする。
- 日本語はコメントと画面表示用の文字列リテラルだけに使用し、Shift_JIS（CP932）へ変換可能な文字とする。
- コメントは原則として日本語で記述する。
- ビルド前に `tools/scripts/convert-src.bat` を実行し、Shift_JISへ変換したCソースとヘッダーファイルを `PC98DEV/SRC/` へコピーする。
- `PC98DEV/SRC/` はGit管理外の生成・ビルド用フォルダーであり、変換済みのCソースを直接編集しない。
- `PC98DEV/` は、Neko Project 21/W内のMS-DOSからHOSTDRV.COMを使って仮想ドライブとして参照する。
- コンパイルはNeko Project 21/W内でLSI C-86を手動実行する。
- C89相当、変数宣言はブロック先頭、`/* */` コメント、Allmanスタイルを使用する。
- 詳細な常設ルールはルートの `AGENTS.md` に従う。

## 98KEYCHK.Cの現在の仕様

- `src/98KEYCHK.C` がプログラム本体である。
- 現在の表示バージョンはVer.1.0である。
- PC-98 BIOSのINT 18h、AH=04hを使い、キーグループ0～15の現在状態を読み取る。
- 押されているキーは、テキストVRAMの属性領域 `A200:0000` にある反転属性ビット `0x04` を変更して反転表示する。
- 通常起動ではPC-9801相当の配置を表示し、WINキーとAPPキーは表示しない。
- `/W` または `/w` を付けて起動するとWIN・APP Modeになり、左右WINキーとAPPキーを表示する。
- 終了操作はCTRL+Cである。キーコード `0x74` と `0x2b` の同時押下を監視する。
- 実行中はCOPYとSTOPの特殊処理を避けるため、INT 05hとINT 06hを一時的にIRETへ差し替え、終了時に元のベクターへ戻す。
- 終了時はDOSのINT 21h、AH=0Ch／AL=06h／DL=FFhで、監視中に蓄積したキーボードバッファーを消去する。
- 起動時と終了時に画面を消去し、プログラム名、バージョン、作者名を表示する。

キー状態の取得から表示までを担当する次の関数には、役割を説明する日本語コメントを記載している。

- `read_key_states()`：BIOSのキーセンスで全キーグループの押下状態を取得する。
- `is_key_pressed()`：取得済みの状態から指定キーの押下を判定する。
- `set_key_reverse()`：指定キーの表示範囲について、テキストVRAMの反転属性を設定・解除する。
- `update_key_display()`：取得したキー状態に合わせて、すべてのキー表示を更新する。
- `clear_key_display()`：すべてのキー表示から反転属性を解除する。

## WIN・APPキーの有効化処理

`set_extended_key_mode()` は、実機でWIN・APPキーを認識させるため、PC-98キーボードのデータポート41hとステータス／制御ポート43hを直接使用する。

- 43hへ `0x17` を出力し、キーボードインターフェースの送信を有効にする。
- 41hへ動作モード設定コマンド `0x95` を送り、ACK `0xFA` を確認する。
- 続けてモード値を送り、ACK `0xFA` を確認する。`0x03` はWIN・APPキー有効、`0x00` は通常モードである。
- 最後の送信完了をTxEMPTYで確認する。
- 43hへ `0x16` を出力し、送信を無効にして受信可能な通常状態へ戻し、エラーをリセットする。
- ACKがIRQ1のBIOS処理に先取りされないよう、送受信中だけ `PUSHF` と `CLI` で割り込みを禁止し、`POPF` で処理前のフラグ状態へ戻す。
- TxRDY、RxRDY、TxEMPTYの待機にはタイムアウトを設け、キーボードから応答がない場合に停止し続けないようにしている。

`send_keyboard_byte()` は、RxRDY後に受信した1バイトがACK `0xFA` であることを前提としている。予期しないキーコードを受信した場合に保存して後で渡し直すような複雑な処理は実装していない。

## 起動時のRET解放待ち

以前、WIN・APP Modeで起動直後からRETが反転表示されたままになるという報告があった。

現在は通常モードとWIN・APP Modeのどちらでも、タイトル表示後に `wait_return_key_released()` を呼び、EXE起動に使ったRETが離れるまで待つ。いったん解放を検出した後、200ミリ秒待って再確認し、キーリピート中の一瞬の解放状態で待機を抜けないようにしている。

WIN・APP Modeでは、RETの解放を確認してから `set_extended_key_mode(0x03)` を実行する。これにより、RETの解放コード `0x9C` を `send_keyboard_byte()` がACKとして読み取って捨てる競合も防ぐ。

以前はタイトル表示後に2秒間待つ処理があったが、現在は廃止している。RETを押し続けている間だけ先へ進まず、離して状態が安定した後にキーボード表示へ移る。

## 終了時のCTRL+C解放待ち

CTRL+Cを検出すると、すべてのキーの反転表示を解除した後、`wait_exit_keys_released()` でCTRLとCの両方が離れるまで待つ。その後、WIN・APP Modeの解除、キーボードバッファー消去、割り込みベクターとBEEP設定の復元を行い、タイトル画面を表示して終了する。

これにより、終了時にCTRL+Cを押し続けている間は終了処理を先へ進めず、キー入力がDOS側へ残りにくくしている。

## キーバッファー溢れBEEPの抑止

実機では、キー状態を監視している間もBIOSキーバッファーへ入力が蓄積し、バッファー溢れのBEEPが鳴る問題があった。

`disable_keyboard_buffer_beep()` はBIOSフラグ `0000:0500h` のD5（`0x20`）を設定してBEEPを抑止する。変更前のD5だけを保存し、終了時に `restore_keyboard_buffer_beep()` で元の状態へ戻す。他のBIOSフラグは変更しない。

## 通常モードの右Shift表示

拡張キーが無効なキーボードでは、左右どちらのShiftを押しても左Shiftのキーコードが届く場合がある。

通常モードの `98KEYBORD.TXT` と `display_keyboard()` では、右Shiftの文字だけを空白にして非表示としている。`key_displays` の右Shift項目 `{0x7d, 4, 32, 5}` は残してあるため、右Shiftのキーコードが届くキーボードでは空白部分が反転表示される。WIN・APP Modeでは右Shiftを表示する。

## 付属説明書と参考画像

- `src/98KEYCHK.TXT` は配布物へ付属させる説明書で、PC-98上で読む可能性があるためShift_JISでGit管理している。
- 対応機種、Normal ModeとWIN・APP Modeの起動方法、CTRL+Cによる終了、左右SHIFT・左右WIN・APPキーの判定仕様、非純正キーボードでの例、テスト済み環境を記載している。
- WIN・APP Modeの説明では、拡張キーコードの送出を有効にして各キーを個別に判定することを記載している。
- `references/PC-98Keyboard.jpg` にPC-98キーボードの参考写真を追加している。

## 今回変更した内容

前回の引き継ぎ書 `20260806_02_引き継ぎ書.md` 以降、次の変更が行われた。

- 起動時にタイトル画面を表示し、RETが離れてからキーボード画面へ進むようにした。
- RET解放後に200ミリ秒待って再確認し、キーリピート中の一瞬の解放を安定した解放と誤認しないようにした。
- 終了時にCTRL+Cが離れるまで待ち、最後にタイトル画面だけを表示して終了するようにした。
- 起動時と終了時に設けていた固定2秒待機を廃止した。
- 正式公開バージョンとしてVer.1.0を設定した。
- Shift_JISの付属説明書 `src/98KEYCHK.TXT` をGit管理へ追加した。
- `README.md` にリポジトリのフォルダー構成を追加し、`src/`、`dist/`、`references/`、`tools/` の説明を整理した。
- `tools/readme.md` にNeko Project 21/W、FreeDOS(98)、FILMTN、LSI C-86、HOSTDRV.COMなどの開発環境構築情報を集約した。
- `.gitignore` を変更し、`dist/` 自体ではなく `dist/package/` と `dist/package_back/` をGit管理外にした。
- `references/PC-98Keyboard.jpg` を追加した。

## ビルド・動作確認結果

- `PC98DEV/SRC/98KEYCHK.C` は、現在の `src/98KEYCHK.C` をShift_JISへ変換した内容とバイト単位で一致している。
- 変換済みCソースの更新日時は2026-08-08 01:18:01、`PC98DEV/SRC/98KEYCHK.EXE` の更新日時は2026-08-08 01:18:27である。
- 引き継ぎ書作成時にはLSI C-86による再ビルドを行っていない。
- 引き継ぎ書作成時にはNeko Project 21/Wおよび実機での追加動作確認を行っていない。
- `README.md` と `src/98KEYCHK.TXT` には、Neko Project 21/W、PC-9821 Ce、PC-9821 Xa10、PC-9821Xa9、および「クラシックPC救済委員会 USB KEYBOARD for PC-98x1」を使用した環境をテスト済みとして記載している。

## 未解決・注意点

- WIN・APPキーの有無は自動判別せず、通常起動と `/W` の手動切り替えとしている。
- 左右WINキーについて、Neko Project 21/W内でもPC-98側の名称表とWindows入力変換表に違いがある。現在の表示と実際の動作は合っているため、98KEYCHK側は `0x77` を左WIN、`0x78` を右WINとしている。
- RETとテンキー側Rは同じキーコード `0x1C` のため、現在のBIOSキーセンス方式では区別できない。
- RET解放待ちとCTRL+C解放待ちにはタイムアウトを設けていない。該当キーを押し続けている間は待機を継続する仕様である。
- `src/98KEYCHK.TXT` はShift_JIS、`src/98KEYCHK.C` はUTF-8である。文字コードを取り違えないこと。
- 日本語コメントと画面表示文字列には、Shift_JIS（CP932）へ変換できない文字を使用しない。
- `tools/scripts/convert-src.ps1` の入力元と出力先はフルパスである。プロジェクトを移動した場合は、スクリプト冒頭の2変数を変更する。
- 変換スクリプトは入力元直下の `.c` と `.h` だけを対象とし、出力先だけに存在するファイルは削除しない。`src/98KEYCHK.TXT` は変換スクリプトの対象外である。
- `PC98DEV/SRC/`、`references/PC9800_BOOKS/`、`references/NP21W_SRC/`、`tools/np21w/`、`dist/package/`、`dist/package_back/` はGit管理外である。明示的な指示または既存の生成・変換処理を除き、変更・削除しない。
- 個人所有のROM、BIOS、MS-DOS入りディスクイメージをGitリポジトリへ追加しない。

## 参考資料

- `references/PC9800_BOOKS/PC-9800シリーズ テクニカルデータブック BIOS編.pdf`
- `references/PC9800_BOOKS/PC-9800シリーズ テクニカルデータブック HARDWARE編.pdf`
- `references/PC9800_BOOKS/PC-9800シリーズ テクニカルデータブック MULTIMEDIA編.pdf`
- `references/PC9800_BOOKS/98を98％使う本 PC-9801 プログラマーズ Bible.pdf`
- `references/NP21W_SRC/` のNeko Project 21/Wソースコード
- `references/LSIC86/` のLSI C-86マニュアル

これらはGit管理対象外である。PDFには透明テキストが付いているが、文字化けしている可能性があるため、重要な表や記述はページ画像でも確認する。

## 現在のGit状態

引き継ぎ書作成前のブランチは `main`、HEADは `3284d30` で、`origin/main` と同期しており、ワークツリーはクリーンだった。

この引き継ぎ書 `docs/handoff/20260808_01_引き継ぎ書.md` の新規作成後は、このファイルのみが未追跡になる。

## 次回作業時に見るべきファイル

- `AGENTS.md`
- `README.md`
- `src/98KEYCHK.C`
- `src/98KEYCHK.TXT`
- `src/98KEYBORD.TXT`
- `src/21KEYBORD.TXT`
- `src/HELLO.C`
- `src/readme.md`
- `dist/readme.md`
- `tools/readme.md`
- `tools/scripts/convert-src.ps1`
- `tools/scripts/convert-src.bat`
- `tools/scripts/convert-src.md`
- `references/readme.md`
- `references/PC-98Keyboard.jpg`
- `references/NP21W_SRC/io/serial.c`
- `references/NP21W_SRC/keystat.c`
- `references/NP21W_SRC/bios/bios09.c`
- `references/PC9800_BOOKS/`
- `PC98DEV/README.TXT`
- `PC98DEV/SRC/README.TXT`
- `.editorconfig`
- `.gitattributes`
- `.gitignore`
- `docs/handoff/20260806_02_引き継ぎ書.md`
