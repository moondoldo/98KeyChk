# 開発環境

このプロジェクトは、PC-98のMS-DOS向けアプリケーションです。

## C(LSI C-86 Ver.3.30c 試食版) コーディング方針

- コンパイラは LSI C-86 Ver.3.30c 試食版を使用します。
- C89相当の古いC言語として記述してください。
- Sモデルでコンパイル可能な規模にしてください。
- C99以降の機能は使用しないでください。
- `//` コメントは使用せず、`/* */` を使用してください。
- 変数宣言はブロックの先頭にまとめてください。
- `stdint.h`、`stdbool.h`、`snprintf`、`long long` は使用しないでください。
- `.c` と `.h` のファイル名および拡張子に含まれる英字は、大文字で記述してください。
- PC-98固有処理とDOS汎用処理を分離してください。
- 必要以上に防御的なコードや、要件にない極端なケースを想定した複雑な実装は避け、要件を満たす読みやすく明示的でシンプルな実装を優先してください。
- ビルドは Neko Project 21/W 内で LSI C-86 を手動実行してください。
- コンパイラやエミュレータ本体のファイルは変更しないでください。
- PC-98エミュレータは `tools/np21w` に配置されています。
- `tools/np21w` 内の実行ファイルや付属ファイルは、明示的な指示がない限り変更しないでください。
- エミュレータの設定ファイルは、設定変更が必要な場合のみ編集してください。
- 個人所有のROM、BIOS、MS-DOS入りディスクイメージはGitリポジトリへ追加しないでください。

## ソースコードの文字コード

- Codexは `src/` 内のUTF-8ソースを編集してください。
- ビルド前に変換スクリプトを実行し、Shift-JISのファイルを `PC98DEV/SRC/` に出力してください。
- `PC98DEV/SRC/` 内の変換済みファイルは直接編集しないでください。

## 別 PC の Codex で最初に確認すること

- `docs/handoff/` の最新引き継ぎ書を読む

## 引き継ぎ書ルール

- 引き継ぎ書は、通常の作業引き継ぎだけでなく、Codex App のプロジェクトを作り直した時の復元資料も兼ねる
- 引き継ぎ書は `docs/handoff/` に作成する
- ファイル名は `yyyyMMdd_NN_引き継ぎ書.md`
- `NN` は `01` から始まる2桁連番
- 日付は作成時点のローカル日付を基本にする
- 同じ日付のファイルが既にある場合は、`docs/handoff/` 内の既存最大連番 + 1 を使う
- ユーザーがファイル名や日付を指定した場合はそれに従う
- すでに同名ファイルがある場合は上書きせず、内容確認または追記方針を相談する
- 引き継ぎ書を新規作成する前に、`docs/handoff/` 内の最新の引き継ぎ書を確認する
- 原則として最新の引き継ぎ書の構成や文体を踏襲する
- 内容には、必要に応じて以下の観点を含める
  - 現在の目的
  - 主要な実装方針
  - 今日変更した内容
  - ビルド確認結果
  - 未解決・注意点
  - 次回作業時に見るべきファイル
- 既存の構成に合わない観点は、無理に見出し化せず本文中に自然に含める

## ファイル編集ルール

- ファイル編集時は `.editorconfig` の書式設定に従う。ただし、改行コードは `.gitattributes` の指定を正とし、同一ファイル内に複数種類の改行コードを混在させない。
- 作業前に対象ファイルやビルドスクリプトが実際に存在するか確認し、存在しないものを前提に変更しないでください。
